# ml-transcript-grader
POC ML project for automated transcript evaluation using a mix of deterministic rules (weighted course scoring) and probabilistic models. Starts with API-based and prebuilt models to validate the concept, then transitions to a trained Gradient Boosting model for more accurate transcript scoring.
